# usage
#inline_script = {
#	script			= buildings/building_eutab_xeno_adaptation_jobs
#	AMOUNT = 200
#}
# Based on jobs/healthcare_add, but triggered (it is assumed there is xenophobe owner)
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			NOT = {
				has_technology = tech_eutab_upgrade_centres
			}
		}
	}
	modifier = {
		job_healthcare_add = $AMOUNT$
	}
}

# Based on jobs/roboticist_add, but triggered (it is assumed there is xenophobe owner)
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_technology = tech_eutab_upgrade_centres
		}
	}
	modifier = {
		job_roboticist_add = $AMOUNT$
	}
}

triggered_desc = {
	trigger = {
		roboticist_is_bath_attendant_individual_machine = yes
		exists = owner
		owner = {
			has_technology = tech_eutab_upgrade_centres
			is_individual_machine = yes
		}
	}
	text = job_toxic_baths_effect_machine_desc_2
}

# Based on jobs/engineers_add, but triggered (it is assumed there is xenophobe owner)
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			# is_regular_empire = yes
			has_technology = tech_eutab_upgrade_centres
			NOT = {
				has_country_flag = fallen_empire_1
			}
		}
	}
	modifier = {
		job_engineer_add = $AMOUNT$
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_technology = tech_eutab_upgrade_centres
			has_country_flag = fallen_empire_1
		}
	}
	job_fe_archivist_add = $AMOUNT$
}
